import Screen from "@/screens/waves";
import type { SeriesScreenProps } from "@/screens/types";
export const dynamic = "force-dynamic";
export default function Page(props:SeriesScreenProps){return Screen(props);}
